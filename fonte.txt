int soma(int a, int b)
{
    int r;
    r = a + b;
    return r;
}

int principal()
{
    int i, j, k;
    float x, y = 2.5;
    int total = 0;

    i = 10;
    j = 3;

    k = i + j * 2 - j / 2;
    x = i + y;

    print(k);
    print(x);

    if (i > j && k != 0) {
        print(1);
    } else {
        print(0);
    }

    k = -i;
    if (!(k < 0)) {
        print(k);
    }

    while (j > 0) {
        {
            total = total + j;
        }
        j = j - 1;
    }
    print(total);

    total = soma(i, j);
    print(total);

    read(i);
    print(i);

    return 0;
}